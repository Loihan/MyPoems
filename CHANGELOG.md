# 更新日志

本项目的所有重要变更都记录在此文件中，最新的变更排在最上方。

---

## 2026-09-18

### 界面重写：书斋风视觉体系

- 全站视觉重做：宣纸底纹、朱砂／青玉／描金点缀、衬线正文配楷体诗词，统一设计令牌（间距、圆角、阴影、动效曲线）。
- **20 套预设主题** + 「跟随系统」：宣纸、月白、青瓷、竹影、雪松、天青、秋香、檀木、藕荷、胭脂；墨夜、松烟、黛绿、苍青、藏蓝、夜雨、绛紫、绛雪、焦茶、玄墨。首屏内联脚本预应用，切换不闪烁。
- 左栏导航、顶栏、搜索、Toast、空状态、加载态全部重做；卡片／胶囊／按钮／表格统一到新体系。

### 新增

- **设置页**（`html/settings.html` + `js/settings.js`）：主题选择、深浅自动切换、阅读器模式（推送／浮层）等偏好集中管理并持久化。
- **自定义对话框**：新增 `App.confirm` / `App.prompt` 取代浏览器原生弹窗——危险操作朱砂警示章、被操作对象单独强调、支持输入框与「仅告知」单按钮；Esc／点遮罩取消、焦点先落在「取消」、回车按当前焦点执行、Tab 在框内循环、关闭后焦点归位。
- **阅读器两种模式**：`push`（宽屏时挤压左侧内容）与 `modal`（浮层居中），支持横排／竖排与对齐方式，左对齐时自动段首缩进（U+3000×2）。
- **跨页联动**：编年史「常用意象」→ 意象簿并选中该字；「摘录 N 句」→ 精句集并切到该年分组；笔耕录体裁／词牌／标签 → 作品总览并预置筛选（`App.openImagery` / `openQuotesOfYear` / `openView` + `setPending`）。
- **案头收藏文件化**：新增 `GET/POST /api/favorites`，收藏写入 `favorites.json`（不再依赖 localStorage），移动端 `db-mock.js` 同步实现。
- **添加新篇**：稿纸样式编辑区、实时字数／段数、草稿芯片（未存／已存）、`Ctrl/Cmd+S` 保存、无实质内容时不再残留空草稿。
- 新增脚本 `scripts/normalize-poem-text.js`：正文留白规范化，默认 dry-run，加 `--write` 才落盘。

### 编年史

- 顶部**年表全景轴**：总篇数／总字数／年数／字每篇，下面是逐年篇数柱（点击跳年，当年高亮）。
- 年卡改为两栏：左栏为年份、篇数、**同比升降**、「今年 · 未完」、类型徽标、**12 格月度小格**；右栏为新用词牌、首次体裁、新启主题、创作跨度、最长篇、总字数与平均、年内最长间隔、摘录入口、常用意象。
- 首尾节点由纯印章改为带说明（「至今 135 篇 · 最近一次落笔 …」／「始于 … 第一首《…》」）。
- 年份小条：独立成列不再遮挡内容、滚动时**吸附在屏幕竖直中央**、自动高亮当前年份、支持 ↑↓ 切换、悬停显示篇数。
- 语义修正：月份雅称表原来整体错位一个月，现为孟春（1 月）…季冬（12 月）。

### 笔耕录

- 与编年史划清边界：**移除「逐年篇数柱状图」**（该维度移交编年史），本页只做跨年聚合。
- 新增**累计字数曲线**、**篇长分布直方图**（按体裁着色）、**全期月份偏好柱图**（峰值标红）、**创作节律**（平均间隔／动笔天数／单日最多／最密一月／最长沉默）。
- 「创作之最」由 4 条扩到 9 条：最长、最短、最早、最近、最高产年、最高产月、单日最多、最长沉默、最常用体裁。
- 体裁分布、词牌一览、主题标签均改为可点击并联动作品总览筛选。

### 精句集

- 新增「今日一句」（按日期播种轮换）、「案头收藏」（写入 JSON 文件）、横向卡片轨道（三份副本无缝循环、双向无限、支持滑动与按钮）、精句墙（按年份／标签／体裁分组，可展开）。

### 意象簿

- 选中意象后左右两栏**严格等高**：左栏按屏幕铺满并对齐整行（比原先多显示一行），右栏内部「XXXX 年 · 含「x」之作」列表自带滚动，面板不会被撑高。
- 作品列表改为**平滑缓动滚动并降速**（每格约原生 55%），不再做任何自动吸附；切换年份自动回到顶部；`prefers-reduced-motion` 下直接定位。
- 修复词语卡片一度被压扁的问题（网格行被容器高度压缩，改用 `align-content: start` + `grid-auto-rows: min-content`）。
- 高频词 Top 50、楷书田字格、年度频次与占比折线图保留并适配新主题。

### 修复

- `page-enter` 动画 `fill-mode: both` 残留 transform，导致 `position: fixed` 弹层定位错乱。
- 竖排诗文居中失效（`margin-inline: auto` 在 `vertical-rl` 下无效）。
- 主题切换后遗留 canvas 报错（`Chart.getChart` 防御式销毁）；页面脚本重复执行（`pageToken` 守卫）。
- 编年史：「今」印章与墨线错位 46px、年份小条压住全景轴 103px、年卡底部三块因共用同一 `grid-area` 互相重叠。
- 对话框：`visibility` 参与过渡导致进度 0 帧仍为 hidden 而无法聚焦、回车语义、单按钮告知框、伪标签 `___UNTAGGED___` 外泄。

### 其他

- `server.js`：端口被占用时给出友好提示（EADDRINUSE）并退出，不再抛栈。
- `scripts/build-www.js`：打包时一并复制 `logo.ico`。
- `.gitignore`：新增忽略 `favorites.json`。

---

## 2026-08-28

### 新增

- **安卓移动端支持**：接入 Capacitor 8 + `@capacitor/filesystem`，可将本项目打包为安卓 APK。
- **双模数据层 `js/db-mock.js`**：手机端读写公开目录 `Documents/MyPoems/` 下的 JSON 文件；桌面端自动失效、继续走 Express，功能不受影响。
- **本地化 Chart.js**：将 CDN 引用改为本地 `js/vendor/chart.umd.js`，离线可用。
- **构建脚本**：新增 `scripts/make-seed.js`、`scripts/build-www.js`，及 `npm run android:prepare` / `android:open` 脚本。

### 修复（手机端适配）

- **索引管理**：由左右分栏改为上下堆叠，标签列表改为横向滚动；批量操作栏固定于屏幕底部，无需滚动即可见。
- **编年史**：年份索引由右侧竖排小字改为顶部横向吸顶条，便于点选年份。
- **精句集**：手机端由多列瀑布流改为单列展示。
- **意象簿折线图**：命中范围由单个数据点扩大到整列，点击/悬停更易触发。

### 文档

- 新增 `ANDROID.md`（打包 / 同步 / 存储权限说明）。
- 重写 `README.md`，补充安卓端与数据同步说明。

---

## 2026-04-19

### 新增

- 首个可用版本。
- 功能页：作品总览、添加新篇、作品管理、索引管理、编年史、精句集、意象簿、笔耕录。
- 三栏桌面布局、深浅色模式、Electron 桌面打包。
